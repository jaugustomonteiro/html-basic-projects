$(document).ready(function () {
  new SlimSelect({
    select: '#exampleFormControlSelect1',
  });

  new SlimSelect({
    select: '#singleOptgroups',
  });

  new SlimSelect({
    select: '#multiple',
  });

  new SlimSelect({
    select: '#multipleOptgroups',
  });
});
